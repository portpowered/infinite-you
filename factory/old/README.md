# Historical Starter

This directory preserves an older sample layout for contributor reference. It
is not the default `agent-factory init` scaffold and is not the current
checked-in starter linked from the package README.

Use [`../`](../README.md) for the current checked-in review-loop starter.
